struct Constants {
    struct API {
        static let productionBaseURL = "api.production.com"
        static let stagingBaseURL = "api.staging.com"
        static let devBaseURL = "api.dev.com"
    }
    
    struct Scheme {
        static let https = "https"
        static let http = "http"
    }
}
